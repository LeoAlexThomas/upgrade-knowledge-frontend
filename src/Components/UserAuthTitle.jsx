import React from "react";

const UserAuthTitle = ({ children }) => {
  return (
    <div className="font-Title text-3xl sm:text-5xl font-semibold text-center mb-8">
      {children}
    </div>
  );
};

export default UserAuthTitle;
